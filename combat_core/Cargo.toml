[package]
name = "combat_core"
version = "0.1.0"
edition = "2024"
description = "Headless, deterministic, engine-agnostic tick-based contested-timeline combat simulation."

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
proptest = "1"
